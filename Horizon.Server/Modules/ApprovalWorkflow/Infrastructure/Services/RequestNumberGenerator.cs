namespace Horizon.Server.Modules.ApprovalWorkflow.Infrastructure.Services
{
    /// <summary>
    /// Generates unique, human-readable request numbers (e.g. "REQ-2026-00001").
    /// Numbers are sequential per year and padded to a fixed width to ensure consistent
    /// sort order. Implementation to be added when the request submission feature is developed.
    /// </summary>
    public class RequestNumberGenerator
    {
    }
}
