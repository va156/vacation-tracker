namespace Horizon.Server.API.Middleware
{
    /// <summary>
    /// Middleware that logs each HTTP request and response summary (method, path, status code,
    /// and elapsed time). Sensitive headers and request bodies are intentionally excluded.
    /// Full implementation to be added when the request telemetry feature is developed.
    /// </summary>
    public class RequestLoggingMiddleware
    {
    }
}
